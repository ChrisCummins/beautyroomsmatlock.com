# Variables set by GitHub
safe: true
lsi: false
pygments: true
source: .

# URL prefix:
prefix: ""

markdown: redcarpet

# Enable HTTP redirects
gems:
  - jekyll-redirect-from

# Ignore patterns
exclude: [scripts/]
